# Track B-1: CNN·전이학습 비전검사 — 실습 과제

> 실습 노트북을 실행한 후, 동일한 노트북 안에 새 셀을 추가하여 과제를 완성하세요.
> **새 라이브러리 설치 불필요 | 과제당 소요 시간: 약 15~20분**

---

## 📒 노트북 01 — 이미지 데이터 탐색

**과제 A (필수)**
실습에서 한 클래스의 이미지를 시각화했습니다.
**다른 클래스** 이미지 3장을 동일한 방식으로 시각화하세요.

**과제 B (필수)**
각 클래스(폴더)의 이미지 수를 세어 가장 많은 클래스와 가장 적은 클래스를 출력하세요.

```python
for class_name in class_names:
    count = len(list((data_dir / class_name).glob('*.jpg')))
    print(f"{class_name}: {count}장")
```

**과제 C (심화·선택)**
이미지 10장을 읽어 가로·세로 크기가 모두 동일한지 확인하세요.

---

## 📒 노트북 02 — 이미지 전처리

**과제 A (필수)**
`RandomRotation(degrees=10)`을 `degrees=30`으로 바꿔 증강된 이미지를 시각화하세요.

**과제 B (필수)**
정규화 전후의 픽셀 값 범위(min, max)를 출력하고 비교하세요.

```python
print(f"정규화 전: min={tensor.min():.2f}, max={tensor.max():.2f}")
print(f"정규화 후: min={normalized.min():.3f}, max={normalized.max():.3f}")
```

**과제 C (심화·선택)**
동일 이미지에 Flip·Rotation·Brightness·Crop 4가지를 각각 적용해 1행 4열 subplot으로 비교하세요.

---

## 📒 노트북 03 — 불량 분류

**과제 A (필수)**
실습에서 사용한 `learning_rate`를 10배 크게 바꾸고 재학습하여 Recall 변화를 출력하세요.

**과제 B (필수)**
Confusion Matrix에서 가장 많이 오분류된 클래스 쌍(행→열)을 찾아 셀 아래에 적어보세요.

```python
# 가장 많이 혼동된 쌍:
# 예) 클래스 A → 클래스 B로 N번 오분류
```

**과제 C (심화·선택)**
`num_epochs`를 절반으로 줄여 Recall이 어떻게 변하는지 비교하세요.

---

## 📌 제출 방법

- 기존 노트북에 과제 셀을 추가하고 전체 실행 후 `.ipynb` 파일 제출
- **제출 기준**: 과제 A·B 실행 완료 + 결과 확인 + 관찰 주석 1줄
